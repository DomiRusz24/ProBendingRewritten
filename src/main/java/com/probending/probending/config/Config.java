package com.probending.probending.config;

import com.probending.probending.ProBending;

public class Config extends AbstractConfig {
    public Config(String path, ProBending plugin) {
        super(path, plugin);
    }
}
